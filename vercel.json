{
  "version": 2,
  "routes": [
    {
      "src": "/api/update/(?<platform>[^/]+)/(?<quality>[^/]+)/(?<commit>[^/]+)",
      "dest": "/api/update?platform=$platform&quality=$quality&commit=$commit"
    },
    {
      "src": "/api/download/(?<platform>[^/]+)/(?<quality>[^/]+)",
      "dest": "/api/download?platform=$platform&quality=$quality"
    }
  ]
}
